module.exports = function reverse(n) {
    var myArr = String(num)
        .split("")
        .reverse()
        .map((num) => {
            return Number(num).toString();
        });
    return myArr.join("");
};
